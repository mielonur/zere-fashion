/** @type {import('next').NextConfig} */
const nextConfig = {
  // A2: Алиасы
  experimental: {},
  // Разрешение внешних изображений
  images: {
    remotePatterns: [],
  },
};

module.exports = nextConfig;
